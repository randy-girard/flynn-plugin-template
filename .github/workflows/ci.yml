name: CI

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

permissions:
  contents: read

env:
  GO_VERSION: "1.24"

jobs:
  test:
    name: Unit tests
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: gofmt
        run: |
          set -eo pipefail
          unformatted="$(gofmt -s -l .)"
          if [[ -n "${unformatted}" ]]; then
            echo "gofmt -s needed on:" >&2
            echo "${unformatted}" >&2
            exit 1
          fi

      - name: Release notes
        run: ./script/test-release-notes.sh

      - name: go test
        env:
          CGO_ENABLED: "0"
          GOFLAGS: "-mod=mod -buildvcs=false"
        run: |
          mkdir -p coverage
          go test -covermode=atomic -coverprofile=coverage/pkg.out ./...
          ./script/report-unit-coverage coverage/pkg.out
